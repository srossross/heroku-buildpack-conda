#!/usr/bin/env bash
# bin/release <build-dir>

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path
BUILD_DIR=$1

cat <<EOF
---
config_vars:
  ROOT_DIR: "/app"
  PATH: /app/.heroku/miniconda/bin:/usr/bin:/bin
default_process_types:
  hello: cat hello.txt

EOF
